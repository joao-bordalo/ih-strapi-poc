const Layout = ({ children, categories, seo }) => (
  <div
    style={{
      width: "1000px",
      padding: "20px",
      margin: "0 auto",
      border: "1px solid red",
    }}
  >
    {children}
  </div>
)

export default Layout
